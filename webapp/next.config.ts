import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Simplified config for development
  images: {
    domains: ['vysninstructionmanuals.web.app', 'images.unsplash.com'],
  },
}

export default nextConfig